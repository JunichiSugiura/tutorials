## Deno REST API Tutorial

### Setup

```sh
$ brew install deno
$ deno install --allow-read --allow-run --allow-write -f --unstable https://deno.land/x/denon/denon.ts
```

### Start server

```sh
$ denon start
```
